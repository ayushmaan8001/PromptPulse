# PromptPulse – Database Configuration
# Copy this file to .env and fill in your values.
# The .env file is git-ignored and must NEVER be committed.

DB_HOST=localhost
DB_PORT=5432
DB_NAME=promptpulse
DB_USER=postgres
DB_PASSWORD=your_password_here
